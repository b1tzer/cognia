# ===== LLM 接入（本地 OpenAI 兼容 adapter）=====
# 本地 adapter 把标准 OpenAI 请求转发到工蜂 Copilot Gateway
# （/opt/tdai/adapter/adapter.py，监听 127.0.0.1:8090）
# 客户端鉴权 key = adapter 进程的 ADAPTER_API_KEY（未配置则放行）
LLM_API_BASE=http://127.0.0.1:8090/v1
LLM_API_KEY=


# ===== 模型 ID =====
# 注意：deepseek-chat / deepseek-reasoner 已于 2026-07-24 退役
# 必须用 external 版（deepseek-v4-flash-external / deepseek-v4-pro）：
# internal 版 deepseek-v4-flash 不返回 reasoning_content（思考链），
# 而 external 版默认返回完整 reasoning_content。
PLANNER_MODEL=deepseek-v4-flash-external
DIAGNOSER_MODEL=deepseek-v4-flash-external
TEACHER_MODEL=deepseek-v4-flash-external

# ===== DeepSeek V4 思考模式 =====
# thinking 开关（true/false），开启后模型先输出 reasoning_content 再给最终答案
THINKING_ENABLED=true
# 推理力度：low / high / max（high 为官方默认；max 需较大输出预算）
THINKING_EFFORT=high

# ===== 数据库（Supabase 托管 Postgres，含 pgvector）=====
# 短期记忆 Checkpointer + 长期记忆 Store 共用
LANGGRAPH_DATABASE_URL=postgresql://postgres:password@localhost:5432/cognia

# ===== 可观测（Langfuse 开源，可选）=====
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=
